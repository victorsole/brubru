# FastAPI & Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database & ORM
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9  # PostgreSQL adapter
supabase>=2.10.0  # Supabase Python client

# Pydantic (validation)
pydantic>=2.10.3
pydantic-settings>=2.1.0

# Authentication & Security
python-jose[cryptography]==3.3.0  # JWT tokens
passlib[bcrypt]==1.7.4  # Password hashing
python-dotenv==1.0.0
stripe==10.0.0  # Stripe payment processing

# AI SDKs
mistralai>=1.0.0  # Mistral (primary - cost-effective)
anthropic>=0.77.0  # Claude (fallback 1)
openai>=2.0.0  # GPT-4 (fallback 2)
google-generativeai>=0.8.0  # Gemini (fallback 3)
google-cloud-translate==3.15.0  # Google Translate API

# Hugging Face & Transformers
transformers==4.36.0  # Hugging Face models
huggingface-hub==0.34.4  # Hugging Face Hub client (required >=0.34.0 for transformers)
torch==2.1.2  # PyTorch backend
accelerate==0.25.0  # Efficient model loading
bitsandbytes==0.41.3  # 8-bit quantization for smaller memory footprint
sentencepiece==0.1.99  # Required for some models
langdetect==1.0.9  # Language detection for translation backfills (MEUB news + Tenderator translations)

# LangChain & RAG
langchain==0.1.0  # RAG orchestration
langchain-community==0.0.10  # Community integrations
langchain-core==0.1.10  # Core langchain
faiss-cpu==1.7.4  # Fast vector similarity search (use faiss-gpu for GPU)

# Web Scraping
aiohttp==3.9.3
beautifulsoup4==4.12.3
lxml==5.1.0
tenacity==8.2.3  # Retry logic
feedparser==6.0.11  # RSS/Atom feed parsing

# Background Tasks & Scheduling
apscheduler==3.10.4  # Background task scheduling for RSS feed polling

# PDF Processing (Phase 1: Content Extraction)
pypdf==4.0.1  # PDF text extraction
pdfplumber==0.11.0  # Advanced PDF parsing with tables
pikepdf==8.11.1  # PDF manipulation and metadata

# DOCX Processing
python-docx==1.1.0  # Word document parsing

# DOCX / PDF / PPTX rendering for /api/documents/{id}/export and the
# document generator (one-pager, press release, impact assessment, etc.)
reportlab>=4.5.0    # PDF rendering for user_documents export
python-pptx>=1.0.0  # Real PPTX rendering for presentation exports

# Vector Database (Phase 2: Semantic Search)
chromadb==0.4.22  # Vector database for semantic search
sentence-transformers==2.3.1  # Embeddings for ChromaDB
pgvector==0.2.4  # PostgreSQL vector extension for embeddings

# ML Models (Phase 2: Predictions)
xgboost==2.0.3  # Gradient boosting for timeline/outcome prediction
scikit-learn==1.4.0  # ML utilities, preprocessing, metrics

# XML Processing (Akoma Ntoso)
lxml==5.1.0

# RDF / DCAT-AP / LegalHTML / EuroSciVoc (EU Vocabularies arc — Phases 2, 8, 12)
rdflib==7.6.0

# YAML manifest parsing (Phase 0 manifest, sync scripts) — declare explicitly.
# Range matches mistralai's transitive requirement.
PyYAML>=6.0.2,<7.0.0

# MCP Toolbox for Databases (Google GenAI Toolbox)
toolbox-core>=0.5.0  # Python SDK for MCP Toolbox server

# Headless-browser fetcher for JS-WAF-walled EU pages. eur-lex.europa.eu returns
# HTTP 202 + empty body to non-JS clients; Playwright/Chromium clears the challenge.
# Used by services/scrapers/waf_browser_fetcher.py as a LAST-RESORT fallback
# (Cellar SPARQL/REST is the primary path).
# NOTE: pip installs the Python package only. The browser binary is separate — the
# Dockerfile must run `python -m playwright install --with-deps chromium` for the
# fetcher to work at runtime (adds ~400MB to the image).
playwright==1.58.0

# Utilities
python-dateutil==2.8.2
pytz==2024.1

# Development
pytest==7.4.4
pytest-asyncio==0.23.3
black==24.1.1
flake8==7.0.0
mypy==1.8.0
