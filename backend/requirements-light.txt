# FastAPI & Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database & ORM
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
supabase>=2.10.0

# Pydantic (validation)
pydantic>=2.10.3
pydantic-settings>=2.1.0
email-validator==2.1.0  # Required for pydantic EmailStr

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0
stripe==10.0.0

# AI SDKs (API-based, no local models)
# mistralai==1.9.11 is installed in the Dockerfile from a local checkout of
# the v1.9.11 tag (PyPI release quarantined; plain git+https install fails on
# missing README-PYPI.md). See the Dockerfile RUN step above the COPY of
# this file. When PyPI un-quarantines, restore the simple pin here and drop
# the Dockerfile clone step.
anthropic>=0.77.0
openai>=2.0.0
google-generativeai>=0.8.0
google-cloud-translate==3.15.0

# Hugging Face Hub (API only, no torch)
huggingface-hub==0.34.4

# LangChain & RAG (lighter)
langchain==0.1.0
langchain-community==0.0.10
langchain-core==0.1.10

# Web Scraping
aiohttp==3.9.3
beautifulsoup4==4.12.3
lxml==5.1.0
tenacity==8.2.3
feedparser==6.0.11
# Headless-browser fallback for JS-WAF-walled EU pages (eur-lex.europa.eu returns
# 202 + empty body to non-JS clients). Used by services/scrapers/waf_browser_fetcher.py.
# The Chromium binary is installed separately in the Dockerfile.
playwright==1.58.0

# Background Tasks
apscheduler==3.10.4

# PDF Processing
pypdf==4.0.1
pdfplumber==0.11.0
pikepdf==8.11.1
pdfminer.six==20231228  # Text extraction for large PDFs
pypdfium2==4.25.0  # Page count detection

# DOCX Processing
python-docx==1.1.0

# Vector Database (use API-based embeddings)
chromadb==0.4.22
pgvector==0.2.4

# OpenTelemetry (pinned to prevent pip backtracking; required by chromadb)
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-proto==1.27.0
opentelemetry-exporter-otlp-proto-common==1.27.0
opentelemetry-exporter-otlp-proto-grpc==1.27.0
opentelemetry-semantic-conventions==0.48b0
opentelemetry-instrumentation==0.48b0
opentelemetry-instrumentation-fastapi==0.48b0
opentelemetry-instrumentation-asgi==0.48b0

# Caching
redis==5.0.1

# Utilities
python-dateutil==2.8.2
pytz==2024.1
numpy==1.26.3  # Required for embeddings and vector operations
scikit-learn==1.4.0  # Required by services/parsers/recital_article_linker (TF-IDF + cosine)

# RDF / DCAT-AP / LegalHTML / EuroSciVoc (EU Vocabularies arc — Phases 2, 8, 12)
rdflib==7.6.0

# YAML manifest parsing (Phase 0 manifest, sync scripts).
# Range matches mistralai 1.9.11's transitive requirement (pyyaml<7.0.0,>=6.0.2).
PyYAML>=6.0.2,<7.0.0

# Excel export for the ?format=xlsx API/MCP export layer (services/export_tabular.py).
# Lightweight (pure-Python + et-xmlfile); without it to_xlsx ImportErrors and the
# export middleware silently falls back to JSON. Keep in sync with requirements.txt.
openpyxl==3.1.5
