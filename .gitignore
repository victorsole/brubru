# docs/ and CLAUDE.md are ignored by default (local notes, drafts).
# Specific files under docs/ are selectively force-added (-f) when they become
# public references (e.g. business plans, application packs, training docs).
docs/
# Brubru design library — generated by /design, screenshot-only scratchpad.
# Never force-add: contains all daily LinkedIn / pitch / deep-dive visuals as
# self-contained HTML sections, regenerated freely.
docs/marketing/designs/
# Brubru feature videos — rendered MP4s + intermediate assets, regenerated locally.
docs/marketing/videos/
data/
# EU Vocabularies manifest must ship in the Railway image (cron sync reads it).
# Narrow exception so the rest of backend/data/ stays ignored.
!backend/data/
!backend/data/eu_vocabularies/
!backend/data/eu_vocabularies/manifest.yaml
brubru.world/
.env
node_modules/
frontend/node_modules/
dist/
build/
frontend/dist/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/
.env.local
.env.*
!.env.example
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
.idea/
.vscode/
.history/
*.swp
*.swo
uploads/
backend/cache/

# Catalan EU legislation translations (28K+ HTML files, uploaded to SiteGround via FileZilla)
data/legislacio-ue-catala/
chroma_db/
# Google Cloud credentials
backend/services/*.json
*.credentials.json

# AI assistant context (local only)
CLAUDE.md
MISTRAL.md
.mcp.json
.claude/
memory/
backend/backend/

# Large files (GitHub limit: 100MB)
*.tif
*.tiff
*.zip
*.tar.gz
*.rar
*.7z
*.db
*.sqlite
*.sqlite3
*.pkl
*.pickle
*.h5
*.hdf5
*.parquet
at4am-project/

# Personal files
victor.JPG

# Private user-specific knowledge guides (20 May 2026).
# Per-user/per-org bespoke briefing material loaded into chat context for the
# authenticated user. NEVER commit — these contain confidential prospect data,
# uploaded documents, scraped EUTR profiles, and bespoke positioning notes.
# Only the README explaining the convention is tracked.
backend/knowledge_base/private_guides/*
!backend/knowledge_base/private_guides/README.md
# Private guides index HTML — generated locally for review, never deployed.
frontend/public/guides/private/

# Client logos (20 May 2026). Local-only — copyrighted third-party logos.
# Carousel on the landing page references files in this folder by name.
# Keep a README explaining what should sit here on a fresh clone.
frontend/public/clients/*
!frontend/public/clients/README.md

# Catalonia campaign send-script + sourced prospect emails (22 Jun 2026).
# Local-only: contains live outreach logic + verified prospect contact emails
# (PII). Never commit. See memory/project_catalonia_startups_campaign_2026_06.md.
backend/scripts/send_catalonia_campaign.py
backend/data/emails/

# Lean repo (24 Jun 2026). Anything NOT needed to build or render Brubru's
# website is ignored. Frontend = Vite build from frontend/src/; backend code +
# migrations + knowledge_base/ stay; data dumps, outreach assets, historical
# audits, top-level vestigial scaffolds, scratchpad scripts are all out.

# Historical audit + log dumps
audit/
logs/

# Outreach data + lead-gen PII (NOT website content)
data/emails/
data/eu_laws_master.csv

# Apify / off-the-radar / campaign scratch dumps under backend/data/
backend/data/apify/
backend/data/catalonia_startups/
backend/data/off_the_radar_*/
backend/data/stripe_*/

# Local scratch scripts (campaign, outreach, fetchers, video pipeline)
backend/scripts/dormant_claim_provision.py
backend/scripts/fetch_stripe_*.py
backend/scripts/send_brubru_brief_*.py
backend/scripts/send_batch_*.py
backend/scripts/videos/

# Top-level vestigial scaffolds + one-off artefacts
/public/
/scripts/
frontend_app_tsx_template.tsx
frontend_header_tsx_template.tsx
frontend_vite_config_template.ts
law_indexing_stats_*.json
unclassified_laws_*.csv
test_hybrid_chat.py
test_scraper.py

# Backup files
*.bak
